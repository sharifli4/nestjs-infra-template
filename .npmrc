# Ignore scripts when running in CI/Docker
# This prevents Husky from trying to install Git hooks in containerized environments
ignore-scripts=false

# Note: In CI/Docker, set HUSKY=0 environment variable to skip Husky entirely
